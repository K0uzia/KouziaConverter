# Converters (à venir)

- `browser/` : conversion dans le navigateur (images, limites `capabilities`)
- `native/` : FFmpeg et outils embarqués (application Tauri)

Non implémenté dans la phase actuelle (interface uniquement).
